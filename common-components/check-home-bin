if [[ ! -d "$HOME/.bin/" ]]; then
***REMOVED***
***REMOVED***

***REMOVED***
  touch $HOME/.zshrc
***REMOVED***

if [[ ":$PATH:" != *":$HOME/.bin:"* ]]; then
  printf 'export PATH="$HOME/.bin:$PATH"\n' >> ~/.zshrc
  export PATH="$HOME/.bin:$PATH"
***REMOVED***
