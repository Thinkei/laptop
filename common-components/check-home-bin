if [[ ! -d "$HOME/.bin/" ]]; then
***REMOVED***
***REMOVED***

***REMOVED***
  touch $HOME/.zshrc
***REMOVED***

if [[ ":$PATH:" != *":$HOME/.bin:"* ]]; then
  echo 'export PATH="$HOME/.bin:$PATH"' >> ~/.zshrc
  export PATH="$HOME/.bin:$PATH"
***REMOVED***
